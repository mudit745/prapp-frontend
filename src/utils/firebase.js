// Placeholder: Firebase removed for BTP migration. Use storage-placeholder for uploads.
// Real storage can be wired to BTP Object Store or backend /api/v1/documents later.
export const storage = Object.freeze({ _placeholder: true })
